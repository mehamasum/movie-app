export default theme => ({
  centerMiddleContainer: {
    zIndex: 1000,
    position: 'absolute',
    top: 'calc(50% - 20px)',
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    textAlign: 'center'
  },
  loader: {
    margin: 'auto'
  }
});
